class Bird:
    def __init__(self, x: int, y: int, config=None):
        if not x or not y:
            raise Exception('x and y parameters are required')
        self.x = x
        self.y = y
        self.v = 0
        if config
        self.g = 
    
